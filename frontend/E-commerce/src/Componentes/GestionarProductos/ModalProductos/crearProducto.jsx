import { Dialog, IconButton, AppBar, Toolbar, Typography } from '@mui/material'
import Grid from '@mui/material/Grid2';
import CloseIcon from '@mui/icons-material/Close';
import { useFetch } from '../../../Request/fetch';
import { HighQuality, Margin } from '@mui/icons-material';

export default function CrearProducto({open, onClose, setEstadoModal}) {
  return (
    <h1>crear producto</h1>

  )
}
